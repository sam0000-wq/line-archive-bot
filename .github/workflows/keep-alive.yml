name: Keep Render Alive
on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render service
        run: curl -sS -o /dev/null -w "%{http_code}" https://line-archive-bot.onrender.com/health || true
